# Sanitized Basic Example

This example shows the shape of an evidence-backed SEO/GEO pass without real
credentials, real analytics data, private URLs, email addresses, or customer
records.

Use `evidence/crawl-summary.json` as local evidence and compare the output
shape with `expected-report.md`. The domain `example.invalid` is reserved for
documentation and is intentionally not a real project.
